import {Link} from 'react-router-dom';import {useTheme} from '../theme/ThemeProvider';import {PRESET_IDS} from '../theme/defaults';export default function Dashboard(){const{theme,loadPreset,save,reset,exportJson}=useTheme();return <div className="min-h-screen p-6"><header className="flex items-center justify-between border-b pb-4"><Link to="/">← Landing</Link><h1 className="text-xl font-bold">Fleet <span className="text-gradient">Theme Studio</span></h1><div className="flex gap-2"><button className="btn-secondary" onClick={save}>Save</button><button className="btn-secondary" onClick={reset}>Reset</button></div></header><main className="mx-auto max-w-5xl py-8"><div className="card"><h2 className="font-bold">Presets</h2><div className="my-4 flex gap-2">{PRESET_IDS.map(id=><button key={id} className="btn-secondary" onClick={()=>void loadPreset(id)}>{id}</button>)}</div><div className="mesh-hero rounded-xl p-8"><p className="text-accent-soft">{theme.brand.tagline}</p><h2 className="text-4xl font-bold">{theme.brand.name} <span className="text-gradient">preview</span></h2><button className="btn-primary mt-6">Primary CTA</button></div><pre className="mt-4 max-h-96 overflow-auto text-xs">{exportJson()}</pre></div></main></div>}
