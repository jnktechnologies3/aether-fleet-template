import {site} from '../content/site';export default function Pricing(){const{pricing}=site;const handleCta=(name:string)=>{window.alert(`Demo only — “${name}” has no real checkout. Thanks for exploring.`)};return <section id="pricing" className="section-pad scroll-mt-24"><div className="mx-auto max-w-6xl"><div className="mb-12 text-center"><h2 className="section-title">{pricing.titleBefore} <span className="text-gradient">{pricing.titleHighlight}</span></h2><p className="section-sub mx-auto mt-4">{pricing.subtitle}</p></div><div className="grid gap-6 lg:grid-cols-3">{pricing.tiers.map(t=><div key={t.name} className={`card flex flex-col ${t.featured?'glow-violet relative':''}`} style={t.featured?{borderColor:'color-mix(in srgb, var(--color-violet) 40%, transparent)',background:'linear-gradient(to bottom, color-mix(in srgb, var(--color-violet) 15%, transparent), transparent)'}:undefined}>{t.featured&&<span className="absolute -top-3 left-1/2 -translate-x-1/2 rounded-full brand-gradient-x px-3 py-0.5 text-xs font-semibold text-white">{pricing.featuredBadge}</span>}<h3 className="text-xl font-semibold">{t.name}</h3><p className="mt-1 text-sm" style={{color:'color-mix(in srgb, var(--color-text) 50%, transparent)'}}>{t.blurb}</p><p className="mt-6 text-4xl font-extrabold tracking-tight">{t.price}</p><ul className="mt-6 flex-1 space-y-3 text-sm" style={{color:'var(--color-text-muted)'}}>{t.features.map(f=><li key={f} className="flex gap-2"><span className="text-highlight">✓</span>{f}</li>)}</ul><button type="button" onClick={()=>handleCta(t.name)} className={`mt-8 w-full ${t.featured?'btn-primary':'btn-secondary'}`}>{t.cta}</button></div>)}</div></div></section>}
