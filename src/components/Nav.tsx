import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {site} from '../content/site';import {useTheme} from '../theme/ThemeProvider';export default function Nav(){const[scrolled,setScrolled]=useState(false);const[open,setOpen]=useState(false);const{theme}=useTheme();const brand=theme.brand.logoText||site.siteName;const[brandMain,brandAccent]=brand.includes(' ')?[brand.split(' ').slice(0,-1).join(' '),brand.split(' ').slice(-1)[0]]:[brand,''];useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>24);window.addEventListener('scroll',onScroll,{passive:true});return()=>window.removeEventListener('scroll',onScroll)},[]);return <header className={`fixed inset-x-0 top-0 z-50 transition-all duration-300 ${scrolled?'glass-strong border-b shadow-lg shadow-black/20':'bg-transparent'}`} style={scrolled?{borderColor:'var(--color-border)'}:undefined}><nav className="mx-auto flex max-w-6xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8"><a href="/" className="flex items-center gap-2.5 group"><span className="flex h-9 w-9 items-center justify-center rounded-[var(--radius-lg)] brand-gradient shadow-lg" style={{boxShadow:'0 8px 24px rgba(var(--color-violet-rgb), 0.3)'}}><svg width="18" height="18" viewBox="0 0 24 24" fill="none" className="text-white"><path d="M12 3L20 18H4L12 3Z" stroke="currentColor" strokeWidth="2" fill="none"/><circle cx="12" cy="15" r="2" fill="currentColor"/></svg></span><span className="font-bold tracking-tight">{brandMain}{brandAccent?<>{' '}<span className="text-accent-soft">{brandAccent}</span></>:null}</span></a><div className="hidden items-center gap-8 md:flex">{site.nav.links.map(l=><a key={l.href} href={l.href} className="text-sm transition hover:opacity-100" style={{color:'var(--color-text-muted)'}}>{l.label}</a>)}<Link to="/dashboard" className="text-sm transition hover:opacity-100" style={{color:'var(--color-text-muted)'}}>Theme Studio</Link><a href={site.hero.primaryHref} className="btn-primary !px-4 !py-2 text-sm">{site.nav.cta}</a></div><button type="button" className="md:hidden rounded-[var(--radius-md)] border p-2" style={{borderColor:'var(--color-border)',color:'var(--color-text-muted)'}} aria-label="Toggle menu" onClick={()=>setOpen(v=>!v)}><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">{open?<path d="M6 6l12 12M18 6L6 18"/>:<path d="M4 7h16M4 12h16M4 17h16"/>}</svg></button></nav>{open&&<div className="border-t px-4 py-4 md:hidden" style={{borderColor:'var(--color-border)',background:'color-mix(in srgb, var(--color-bg-deep) 95%, transparent)'}}><div className="flex flex-col gap-3">{site.nav.links.map(l=><a key={l.href} href={l.href} className="rounded-[var(--radius-md)] px-3 py-2 hover:bg-white/5" style={{color:'var(--color-text-muted)'}} onClick={()=>setOpen(false)}>{l.label}</a>)}<Link to="/dashboard" className="rounded-[var(--radius-md)] px-3 py-2 hover:bg-white/5" style={{color:'var(--color-text-muted)'}} onClick={()=>setOpen(false)}>Theme Studio</Link><a href={site.hero.primaryHref} className="btn-primary text-center text-sm" onClick={()=>setOpen(false)}>{site.nav.cta}</a></div></div>}</header>}
